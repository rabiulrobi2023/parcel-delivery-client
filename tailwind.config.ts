import { Config } from "tailwindcss";

export default {
  theme: {
    extend: {
      colors: {
        firstColor: "#be927c",
        secondColor: "#374f68",
      },
    },
  },
} satisfies Config;
